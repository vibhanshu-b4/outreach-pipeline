# outreach_pipeline

Overview: minimal scaffold for an outreach data pipeline. Contains staged modules, a `data/` folder for JSON inputs, and a `logs/` directory for runtime logs. Configuration values are stored in a `.env` file (ignored by git).
